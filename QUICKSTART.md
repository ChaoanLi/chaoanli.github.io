# 快速启动指南 / Quick Start Guide

## 一分钟快速上手

### 1️⃣ 安装依赖
```bash
npm install --legacy-peer-deps
```
> 注意：使用 `--legacy-peer-deps` 标志以确保与 React 19 的兼容性

### 2️⃣ 启动开发服务器
```bash
npm run dev
```

### 3️⃣ 打开浏览器
访问 `http://localhost:5173` 即可看到您的网站！

## 🎯 下一步

### 修改个人信息
打开 `constants.ts` 文件，修改其中的内容：
- 姓名、职位、单位
- 邮箱和社交媒体链接
- 研究兴趣
- 论文列表
- 时间线事件
- 博客文章

### 部署到 GitHub Pages

#### 方法 1：自动部署（推荐）
1. 将代码推送到 GitHub
```bash
git add .
git commit -m "Update personal information"
git push origin main
```

2. 在 GitHub 仓库设置中启用 GitHub Pages
   - 进入 Settings > Pages
   - Source 选择 "GitHub Actions"
   - 网站会自动部署！

#### 方法 2：手动构建
```bash
# 构建生产版本
npm run build

# dist 文件夹中的内容就是您的网站
```

## 📝 常用命令

| 命令 | 说明 |
|------|------|
| `npm install --legacy-peer-deps` | 安装依赖 |
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |

## ⚙️ 配置说明

### 修改网站标题和图标
编辑 `index.html` 文件：
```html
<title>您的名字 - 个人网站</title>
```

### 修改基础 URL（如果需要）
如果您的网站地址是 `username.github.io/repo-name`，需要修改 `vite.config.ts`：
```typescript
base: '/repo-name/',  // 改为您的仓库名
```

如果是 `username.github.io`，保持：
```typescript
base: '/',
```

## 🎨 自定义样式

网站使用内联 Tailwind CSS 类进行样式控制。主要组件文件：
- `App.tsx` - 主页面布局
- `components/Timeline.tsx` - 时间线组件
- `components/PublicationCard.tsx` - 论文卡片
- `components/BlogSection.tsx` - 博客部分
- `components/ResourceLibrary.tsx` - 资源库
- `components/VisitorStats.tsx` - 访客统计

## ❓ 常见问题

**Q: 如何更改网站语言？**
A: 网站默认支持中英文切换，点击右上角的语言切换按钮即可。

**Q: 如何添加更多论文？**
A: 在 `constants.ts` 的 `publications` 数组中添加新的论文对象。

**Q: 如何修改访客统计数据？**
A: 在 `constants.ts` 的 `stats` 对象中修改数据。如需真实的访客统计，可以集成 Google Analytics 或其他分析工具。

**Q: 部署后页面显示404？**
A: 检查 `vite.config.ts` 中的 `base` 配置是否正确。

## 🔗 相关文档

- [完整部署指南](./DEPLOY.md)
- [Vite 官方文档](https://vitejs.dev/)
- [React 官方文档](https://react.dev/)

## 💡 提示

- 修改代码后，开发服务器会自动刷新
- 所有个人信息都在 `constants.ts` 中集中管理
- 项目已配置好 TypeScript，享受类型安全带来的便利
- 可以在 `public` 文件夹中放置静态资源（图片、PDF等）

---

遇到问题？查看 [DEPLOY.md](./DEPLOY.md) 或提交 Issue。

